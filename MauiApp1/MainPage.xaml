<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.MainPage"
             BackgroundColor="#1E1E1E">
    <ScrollView>
        <VerticalStackLayout Padding="30" Spacing="20">
            <!-- Title -->
            <Label Text="Home"
                   FontSize="36"
                   FontAttributes="Bold"
                   TextColor="Lavender"
                   HorizontalOptions="Center" />

            <!-- Phone Number Input Section -->
            <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto" BackgroundColor="#262626"  Padding="10">
                <!-- Country Selection with Flag -->
                <Frame Grid.Column="0"
                       CornerRadius="10"
                       Padding="5"
                       BackgroundColor="Purple">
                    <Grid ColumnDefinitions="Auto,Auto" RowDefinitions="Auto">
                        <Image x:Name="SelectedFlag" Source="uk.png" Grid.Column="0" Margin="5" />
                        <Button x:Name="CountryButton" Text="▼" FontSize="Small" Clicked="OnCountryButtonClicked" Grid.Column="1" TextColor="White" BackgroundColor="Transparent" />
                    </Grid>
                </Frame>

                <!-- Entry for entering phone number -->
                <Entry x:Name="PhoneNumberEntry"
                       Grid.Column="1"
                       Placeholder="Your number"
                       Keyboard="Telephone"
                       TextChanged="OnPhoneNumberTextChanged"
                       TextColor="White"
                       BackgroundColor="Transparent" />
            </Grid>

            <!-- Searchable Country List (Initially Hidden) -->
            <Frame x:Name="CountryListFrame"
                   IsVisible="False"
                   CornerRadius="12"
                   BorderColor="LightGray"
                   BackgroundColor="#1E1E1E"
                   Padding="10">
                <StackLayout>
                    <Entry x:Name="SearchEntry" Placeholder="Search" TextChanged="OnSearchTextChanged" BackgroundColor="#262626" TextColor="White" />
                    <CollectionView x:Name="CountryListView" ItemsSource="{Binding Countries}" SelectionChanged="OnCountrySelectionChanged">
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Grid ColumnDefinitions="Auto,*" Padding="10">
                                    <Image Source="{Binding Flag}" Grid.Column="0" WidthRequest="30" HeightRequest="20" />
                                    <Label Text="{Binding Name}" Grid.Column="1" TextColor="White" FontSize="16" />
                                    <Label Text="{Binding Code}" Grid.Column="1" HorizontalOptions="End" TextColor="Gray" />
                                </Grid>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </StackLayout>
            </Frame>

            <!-- Validation Message -->
            <Label x:Name="ValidationMessage"
                   TextColor="Red"
                   FontAttributes="Bold"
                   FontSize="14"
                   IsVisible="False" />

            <!-- Counter Button -->
            <Button x:Name="CounterBtn"
                    Text="Click me"
                    BackgroundColor="Purple"
                    TextColor="White"
                    CornerRadius="15"
                    HeightRequest="50"
                    FontAttributes="Bold"
                    Clicked="OnCounterClicked"
                    HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>
