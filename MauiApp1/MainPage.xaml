<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://schemas.microsoft.com/dotnet/2021/maui"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             x:Class="MauiApp1.MainPage"
             BackgroundColor="#1E1E1E">
    <ScrollView>
        <VerticalStackLayout Padding="30" Spacing="20">
            <Label Text="Home"
                   FontSize="36"
                   FontAttributes="Bold"
                   TextColor="Lavender"
                   HorizontalOptions="Center" />
            <Grid ColumnDefinitions="Auto,*" RowDefinitions="Auto" BackgroundColor="#262626" Padding="10">
                <Border Grid.Column="0"
                        StrokeShape="RoundRectangle 10"
                        Stroke="Purple"
                        BackgroundColor="Purple"
                        Padding="5">
                    <Grid ColumnDefinitions="Auto,Auto" RowDefinitions="Auto">
                        <Image x:Name="SelectedFlag" Source="uk.png" Grid.Column="0" Margin="5" WidthRequest="30" HeightRequest="20"/>
                        <Button x:Name="CountryButton" Text="▼" FontSize="Small" Clicked="OnCountryButtonClicked" Grid.Column="1" TextColor="White" BackgroundColor="Transparent" />
                    </Grid>
                </Border>
                <Entry x:Name="PhoneNumberEntry"
                       Grid.Column="1"
                       Placeholder="Your number"
                       Keyboard="Telephone"
                       TextChanged="OnPhoneNumberTextChanged"
                       TextColor="White"
                       BackgroundColor="Transparent" />
            </Grid>
            <Border x:Name="CountryListBorder"
                    IsVisible="False"
                    Stroke="LightGray"
                    StrokeShape="RoundRectangle 12"
                    BackgroundColor="#1E1E1E"
                    Padding="10">
                <VerticalStackLayout>
                    <Entry x:Name="SearchEntry" Placeholder="Search" TextChanged="OnSearchTextChanged" BackgroundColor="#262626" TextColor="White" />
                    <CollectionView x:Name="CountryListView" ItemsSource="{Binding countries}" SelectionChanged="OnCountrySelectionChanged">
                        <CollectionView.ItemTemplate>
                            <DataTemplate>
                                <Grid ColumnDefinitions="Auto,*,Auto" Padding="10">
                                    <Image Source="{Binding Alpha2, StringFormat='{0}.png'}" Grid.Column="0" WidthRequest="30" HeightRequest="20" />
                                    <Label Text="{Binding Name}" Grid.Column="1" TextColor="White" FontSize="16" />
                                    <Label Text="{Binding CallingCode}" Grid.Column="2" TextColor="Gray" HorizontalOptions="End" />
                                </Grid>
                            </DataTemplate>
                        </CollectionView.ItemTemplate>
                    </CollectionView>
                </VerticalStackLayout>
            </Border>
            <Label x:Name="ValidationMessage"
                   TextColor="Red"
                   FontAttributes="Bold"
                   FontSize="14"
                   IsVisible="False" />
            <Button x:Name="CounterBtn"
                    Text="Click me"
                    BackgroundColor="Purple"
                    TextColor="White"
                    CornerRadius="15"
                    HeightRequest="50"
                    FontAttributes="Bold"
                    Clicked="OnCounterClicked"
                    HorizontalOptions="Fill" />
        </VerticalStackLayout>
    </ScrollView>
</ContentPage>